# Moonshine V2.1.1 — focused hardware testing

This is a bugfix pass, not a full regression run. Keep normal V2.1 SD data in
place; do not rename or delete existing Moonshine/Susamune data.

Record `PASS`, `FAIL`, `BLOCKED`, or `NT`. For a failure, include the region,
hardware, and shortest reproduction.

| ID | Test | Expected result | Result |
|---|---|---|---|
| T01 | Rename the existing `apps/susamune_launcher` folder to `apps/moonshine_launcher`, extract the ZIP over it, and cold boot. | Exactly one HBC entry appears. It shows the supplied Moonshine banner and **Moonshine Launcher**; the launcher's lower-right build line reads **Moonshine V2.1.1 "Hot Streak".** The ZIP contains `moonshine_launcher`, not `susamune_launcher`. No RC or DELTA label appears. | |
| T02 | Boot with the renamed app folder and existing SD data, confirm any custom launcher theme/BGM, open the in-game menu/Records, change one harmless setting, close the menu, and reboot. | The custom theme/BGM and all existing settings, PBs, split stats, Records, ghosts, playlists, and targets remain. The changed setting persists. Root-level `susamune.ini` and `susamune_*` data need no rename. | |
| T03 | Start Pinna 8 with its Shine already collected. | Mario starts at the normal park entrance, not at the Shine-spawn cutscene. | |
| T04 | Enter Pinna 8's rollercoaster and ride for at least ten seconds. Repeat twice. | The ride does not restart a few seconds after entry. | |
| T05 | Finish Pinna 8 with a time that should PB, then reopen its IL. | The PB saves and is still present after re-entering the IL and after rebooting. | |
| T06 | In Bianco 2, trigger the rollout split and then enter Petey's cutscene. Test both episode 1 and episode 2 starts if practical. | Exactly two splits occur: rollout, then accepted cutscene start. QFT and split freeze on the same cutscene edge. | |
| T07 | Trigger one known loading-zone split with a PB baseline. | The checkpoint is no longer one frame late. Its delta appears directly to the right of the QFT freeze, never underneath it. | |
| T08 | On JP, produce a positive/behind split delta. | A normal `+` sign is visible before the red positive delta; it is not blank and not a controller glyph. | |
| T09 | Turn **Level splits** off, complete a segmented IL, then turn it back on and inspect Segments. | No split overlay appears while off, but segment/PB/gold statistics were still recorded. Turning it on restores the overlay. | |
| T10 | Start a multi-finish Streak with **Streak auto-reset** on and complete a non-final attempt. | Existing behavior remains: the next attempt starts automatically without waiting on the save box. | |
| T11 | Repeat with **Streak auto-reset** off. Wait on the save box, then dismiss/leave it. | The game can remain on the save box indefinitely. Leaving it starts exactly one new attempt at the selected IL start; there is no double reset. | |
| T12 | Display the Sunshine timer during Pianta 6's cleaned counter, Pinna 8's balloon counter, and one red-coin counter. | The timer moves upward while each mission counter is visible, never overlaps it, and returns to the configured position afterward without drifting. | |
| T13 | On PAL, enter Delfino Plaza, stand in normal gameplay for at least ten seconds, then load one stage and return to Plaza. | No crash occurs and the Sunshine timer remains at its normal position while no mission counter is present. | |

## Automated verification already completed

- 156 focused host tests passed.
- All changed game-side sources compiled for JP, US, and PAL.
- All three regional manifests linked and packed successfully.
- The Wii loader and ARM kernel built successfully.
- The package contains one launcher, the exact supplied PNG, metadata,
  changelog/testing log, and JP/US/PAL mod binaries.
