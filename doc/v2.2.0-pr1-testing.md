# Moonshine V2.2.0 Pre-Release 1 "The House Always Wins" - testing

Record `PASS`, `FAIL`, `BLOCKED`, or `NT`. For a failure, include the disc
region, Wii or Dolphin, and the shortest reproduction.

| Field | Value |
|---|---|
| Tester | |
| Disc region | JP / US / PAL |
| Platform | Wii / vWii / Dolphin |
| Build shown | |

## Required checks

| ID | Test | Expected result | Result |
|---|---|---|---|
| T01 | Cold boot from the packaged app and inspect Homebrew Channel, the launcher, and the Records page. | Homebrew Channel and the launcher show `Moonshine Launcher ZETA`; the release/build text identifies `Moonshine V2.2.0 Pre-Release 1 "The House Always Wins"`; Records shows `Moonshine V2.2.0 PR1`; and the packaged components use one matching Build ID. | |
| T02 | Open Settings > Binds and scroll through the whole page. Use C-stick left/right from several rows. | Six labelled sections render without overlap; vertical scrolling keeps the selected bind visible; left/right jumps to the prior/next section. | |
| T03 | Record, clear, cancel, and save one bind near a section boundary. Reopen the menu. | Recording behavior and persistence are unchanged. | |
| T04 | On JP, render text containing `/` on each long menu and overlay path. | Slashes render with the JP-safe glyph and no text corruption occurs. | |
| T05 | Save and load in a normal stage, during a materialize-in wipe, after NPC talk, and from the pause menu. | Existing same-scenario savestate behavior is unchanged. | |
| T06 | Run one short segmented IL and one ghost attempt. | Splits, QFT, PB recording, ghost recording/playback, and result persistence match Moonshine V2.1.1. | |
| T07 | Boot JP, US, and PAL from their normal disc/ISO paths. | Every region reaches gameplay and all menu pages open. | |
| T08 | JP personal build only: inspect Input, Metadata, QFT, recent-IL, savestate-feedback, wallkick, and menu colours. | The gold, ivory, emerald, crimson, and felt-black theme is readable against bright and dark scenes. | |
| T09 | Boot with no `theme/background.png`, then with a valid 1024x480 PNG. | The texture-free stock view matches Moonshine V2.1.1; the custom panorama loads, pans, and darkens normally. | |
| T10 | Try an oversized, truncated, wrong-sized, and non-PNG background. | Each invalid custom file falls back to stock with a useful warning; the launcher remains bootable and leak-free across retries. | |
| T11 | Use Reset from gameplay twice without power-cycling, then reopen every menu page. | The mod reinjects cleanly each time; zero-initialized state does not retain values from the previous run. | |
| T12 | Trigger and save a crash report after an old A/B report pair already exists. | Generation numbering, binary output, and text output remain valid after the 2 KiB kernel-buffer reuse. | |
| T13 | On SD and USB, scan and boot an ISO whose directory/name contains valid 2-, 3-, and 4-byte UTF-8. Also display accented and non-Latin launcher text. | The path is neither truncated nor rejected, supplementary characters survive the FAT round trip, and text renders or uses the font's fallback glyph without a blank line, crash, or per-frame heap growth. | |
| T14 | Exercise several accumulating status screens and an interactive prompt in 4:3, 16:9, PAL interlaced, and progressive modes. | Retained EFB content remains visible until explicitly replaced; text neither disappears nor smears, and no stale frame returns after the prompt. | |
| T15 | Play and loop a near-4 MiB `theme/theme.mp3`, then boot a game and immediately save/load a savestate. Repeat once after returning to the launcher. | Music stops cleanly, ASND joins, game audio starts normally, and the released/cache-invalidated MEM2 overlay never corrupts the game or snapshot. | |
| T16 | Boot the packaged launcher into gameplay twice, including one in-session reset, while watching the kernel-loading status. | Direct kernel inflation reaches gameplay with no copy-stage corruption, size/CRC error, stale kernel bytes, or reset-only failure. | |
| T17 | Compare the default `-Os` build with an `-O3` override while navigating every launcher page and scanning/hashing the same SD, USB, disc, and ISO set. | UI input remains responsive and no scan, hash, mount, or boot step has a material latency regression. | |
| T18 | Cold-boot Wii and vWii several times and inspect all launcher text before and after device remount/IOS reload. | The chunked IPL-font read produces the complete, stable system font with no missing range, cache artifact, or intermittent corruption. | |
| T19 | Decode visually identical 1024x480 RGBA PNGs encoded once non-interlaced and once Adam7-interlaced; pan each fully, then try a truncated variant. | Both valid files render identically across every row/pass, and the truncated file falls back cleanly. The 4,224-byte row-stack frame does not disturb the fixed 128 KiB loader stack. | |
| T20 | With a normal 2 MiB IPL, exercise each available supported revision with progressive/sharp combinations and jingles 1/2; then boot an available Triforce title/Segaboot and create its expected save file(s). | Language, PAL animation, video filter, region, and jingle patches match Moonshine V2.1.1. Triforce detection still selects the correct title/save action, and the DIMM overlay leaves both normal IPL and Triforce boot paths uncorrupted. | |
| T21 | Open Settings > RNG Control, inspect every section, change all six rows, close the menu, and reopen it after a reboot. Also inspect Gameplay and Practice. | Yoshi Eggs, Patterns, King Boo, Petey, and Ricco Crane labels render cleanly. All six settings persist, and the two moved settings no longer appear on their old pages. New post-favorites rows do not show `X Shine` or produce a favorite toast. | |
| T22 | Cold-boot the packaged app into JP and PAL gameplay three times each on Wii hardware. | The launcher passes `Nintendont kernel looping, loading game...`, destroys its font cleanly, and reaches gameplay every time without a DSI or reload. | |
| T23 | Enable `Always rolls fruit`, enter the King Boo fight, and complete at least ten roulette pulls. Then disable it and pull again. | Every enabled pull visibly settles on fruit and generates the normal fruit/pepper set. Disabling it restores normal roulette outcomes. | |
| T24 | In Bianco 5, enable both Petey controls and play through at least one full cycle after N1. Repeat after saving/loading at each landing, then disable both controls. | Petey goes to N1 first, then lands at S1, S2, and S3 in that order and never uses the tornado attack. A longer fight uses N4 only as a non-landing transit. Save/load preserves the next destination; disabling both controls restores retail behavior. | |
| T25 | In several Ricco episodes, test all five speed choices while watching all three rotating cranes and both vertical cranes. Change choice while the cranes are moving, leave/re-enter the stage, and finally select Off. | Every crane keeps one fixed speed rather than jittering. Slowest/Slow/Medium/Fast/Fastest fall within 1-20/21-40/41-60/61-80/81-100% of native maximum; cranes may differ within a band. A live choice change remaps the same roll, a fresh stage rolls again, and Off restores each crane's retail behavior. | |
| T26 | With every new RNG option Off, compare ordinary King Boo, Bianco 5 Petey, and Ricco crane behavior with V2.1.1 on each available region. | No new control changes gameplay while Off, and the retail RNG stream is not shifted by an extra call. | |

## Sign-off

| Field | Value |
|---|---|
| Required tests passed | / 26 |
| Open failures | |
| Tester recommendation | Continue / Hold |
| Notes | |
